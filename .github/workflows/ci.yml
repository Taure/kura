name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'guides/**'
      - 'LICENSE'
      - '.gitignore'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'guides/**'
      - 'LICENSE'
      - '.gitignore'

  # paths-ignore means a docs-only stretch produces no CI run at all, so a
  # dependency moving underneath a pin goes unnoticed. A weekly run catches
  # rot that no commit would have triggered.
  schedule:
    - cron: '11 5 * * 1'
  workflow_dispatch:
jobs:
  ci:
    uses: Taure/erlang-ci/.github/workflows/ci.yml@v2.1.7
    permissions:
      contents: write
      pull-requests: write
    with:
      version-file: '.tool-versions'
      otp-matrix: '["28", "29"]'
      enable-ct: true
      enable-ex-doc: true
      enable-hank: true
      enable-sheldon: false
      enable-audit: true
      enable-coverage: true
      enable-sbom: false
      enable-sbom-scan: false
      enable-dependency-submission: true
      enable-summary: true
      # Diff-scoped: only lines a PR changes are mutated, so this gates
      # new code without a retroactive sweep of every existing test.
      enable-mutate: true
      mutate-diff: true
      mutate-min-score: 75
      enable-elp-lint: true
      enable-elp-eqwalize: false

      extra-services-compose: 'docker-compose.ci.yml'
