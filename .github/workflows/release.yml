name: Release
on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'guides/**'
      - 'LICENSE'
      - '.gitignore'

jobs:
  release:
    uses: Taure/erlang-ci/.github/workflows/release.yml@v1
    permissions:
      contents: write
